@model IEnumerable<_1721050522_Trinh_Thu_Huong_BTL_LTQL.Models.DUAN>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CHITIETDUANs.TenDuAn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaNV)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CHITIETDUANs.TenDuAn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaNV)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TenDuAn }) |
            @Html.ActionLink("Details", "Details", new { id=item.TenDuAn }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TenDuAn })
        </td>
    </tr>
}

</table>
