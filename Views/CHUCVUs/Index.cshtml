@model IEnumerable<_1721050522_Trinh_Thu_Huong_BTL_LTQL.Models.CHUCVU>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaCV)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaCV)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TenCV }) |
            @Html.ActionLink("Details", "Details", new { id=item.TenCV }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TenCV })
        </td>
    </tr>
}

</table>
