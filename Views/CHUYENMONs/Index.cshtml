@model IEnumerable<_1721050522_Trinh_Thu_Huong_BTL_LTQL.Models.CHUYENMON>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaCM)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaCM)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TenCM }) |
            @Html.ActionLink("Details", "Details", new { id=item.TenCM }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TenCM })
        </td>
    </tr>
}

</table>
