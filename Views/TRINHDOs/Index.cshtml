@model IEnumerable<_1721050522_Trinh_Thu_Huong_BTL_LTQL.Models.TRINHDO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaTrinhDo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaTrinhDo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TenTrinhDo }) |
            @Html.ActionLink("Details", "Details", new { id=item.TenTrinhDo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TenTrinhDo })
        </td>
    </tr>
}

</table>
